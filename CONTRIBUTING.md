# Contributing

openFAD is Chinese-first. User-facing labels, quickstarts, and error recovery copy should be written in Simplified Chinese first.

Before opening a pull request:

```bash
npm run scan:public
npm test
```

Keep changes scoped to one tool or one shared package.


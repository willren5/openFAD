# Cover Machine v0.1.1 Verification

Date: 2026-06-13

## Fresh Command Evidence

```bash
npm test
```

Output summary:

- Cover Machine: `tests 1`, `pass 1`, `fail 0`.
- MV Studio: `tests 288`, `pass 288`, `fail 0`.
- Motion Batch: `tests 488`, `pass 488`, `fail 0`.
- Root scripts: `tests 8`, `pass 8`, `fail 0`.

```bash
npm run scan:public
```

Output summary:

- `public safety scan passed`

## Release Artifact Evidence

- `openfad-cover-machine-0.1.1.zip` is generated by `npm run release:prepare`.
- The package includes root `LICENSE`, `NOTICE`, and `TRADEMARKS.md`.
- Final size and SHA256 are recorded in the uploaded `SHA256SUMS` and `release-manifest.json`.

## Covered Behaviors

- 中文首屏 and Start Mode.
- Public-safe demo copy and assets.
- Export dispatch and validation panels.
- Autosave, custom presets, font controls, large background uploads, and project JSON metadata.

## Known Limits

- Browser renderer and browser save behavior can vary.
- FAD Records brand assets are not open licensed.
